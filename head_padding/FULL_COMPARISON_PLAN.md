# Full real-CT comparison, frozen before full padded runs

20 September2026. The tiny first-call probe used actual teacher QKV, not random tensors. Width56 and64 both enabled efficient attention with identical observed first-call outputs; choose the minimum multiple-of-eight width56 for the candidate. This is established padding, not a new attention algorithm. Public code/PR audit found no corresponding Dinovol implementation; unpublished tierval work cannot be ruled out. Current Villa23c3d759 changes only website data from the measured f07d33 baseline, so the consumer and model remain identical.

Use the already verified PHerc0139 256³ CT crop, official Paris4-trained ps8 teacher and reference embedding. Keep original hashes, normalization, bf16 precision, eight128³ windows, stride128, minibatch1, Gaussian sigma4, model parameters and all24 blocks. No training or production source edits. No dependency installs. Existing3090 only; allocator cap85% as in prior diagnostic.

Run three fresh processes in fixed order: unchanged native SDPA, padding to56 with original54**-0.5 scale and native automatic backend selection, and identical padding forced to math as a mechanism control. Two warmups and five synchronized timed `_dino_similarity` calls per arm; profiler pass separate. The candidate's timing includes padding/slicing. Record median,p90,peak allocated/reserved memory and all192 actual attention operators. Startup and CPU input/output transfers remain separately excluded as documented by the existing baseline. A useful performance screen requires at least20% lower median complete-similarity time, finite outputs and no increased peak allocated memory; this does not establish whole-training acceleration.

Record final per-window patch tokens, the blended grid before interpolation, unnormalized interpolated similarity range, and final per-cube minmax-normalized map. Quality captures occur only during the separate profiler pass, after all timing and timed-memory measurements. Check exactly64 model window calls(2warmups+5timed+1profile,8windows each); capture only the last8. Profiling includes these additional transfers, so do not use profiler total time as a throughput measure. Record max/mean absolute error,RMSE,p99/p99.9,relativeL2 and token cosines. Require the padded-math control to be exactly equal at these stages before calling the tested padding mechanism numerically unchanged under math.

Fused kernels need not be bitwise equal. Report all differences, without selecting tolerance thresholds after outcomes or silently enabling the option. Report similarity>0.5 eligibility changes only; this is NOT the production pseudo-label. Production v1/v2 uses sigmoid of the phase-specific frozen U-Net multiplied by similarity, threshold>0.5(v2 also strict raw>50). The exact60k/63k teachers have not yet been found; later64k/77k/78k checkpoints cannot substitute. If exact teachers become available, freeze their probabilities and require zero changed production decisions to claim unchanged labels on this fixture. Otherwise state that label equivalence/ink accuracy remains untested.

Successful first-cube performance remains development evidence. A publication-quality performance claim requires further preselected real cubes and interleaved order to reduce warmup/order effects, plus tested opt-in dispatch/default fallback. No claim of new readable surface, recovered text, Linux/H100 benefit, backward/training validation or any prize amount follows from this experiment.
